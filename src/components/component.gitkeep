import { FC, ReactNode, useEffect } from "react"

const Component: FC<{ children?: ReactNode | ReactNode[] }> = ({ children }) => {
  useEffect(() => {}, [])

  return <></>
}

export default Component
